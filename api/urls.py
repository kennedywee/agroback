from django.urls import path
from . import views

urlpatterns = [
    # Read Data
    #path('device/', views.DataList.as_view(), name='data-list'),

    # Update Data
    #path('update/', views.CreateData.as_view(), name='data-add'),
]
